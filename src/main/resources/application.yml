server:
  port: 9090

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/hellogerty?serverTimezone=UTC&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: Sebastian0821
mybatis:
  mapper-locations: classpath:mapper/*.xml #扫描所有mybatis文件
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
